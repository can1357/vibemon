# Repo-root uv workspace. The Python SDK lives in `python/`, but this root
# table makes `uv` resolve that project from the repository root, so a single
# `.venv` serves both `uv run <cmd>` here and `cd python && uv run <cmd>`.
[tool.uv.workspace]
members = ["python"]
